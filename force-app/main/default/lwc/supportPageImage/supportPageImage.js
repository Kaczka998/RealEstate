import { LightningElement } from 'lwc';
import myResource from '@salesforce/resourceUrl/RE_SupportTeam';

export default class SupportPageImage extends LightningElement {
    supportImage = myResource;
}