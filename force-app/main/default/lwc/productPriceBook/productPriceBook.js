// import { LightningElement, api, wire, track } from "lwc";
// import getRecordsByRecordType from "@salesforce/apex/RE_productController.getRecordsByRecordType";

// const columns = [
//   { label: "Premise Name", fieldName: "Name", editable: false },
//   { label: "Price", fieldName: "UnitPrice", type: "currency", typeAttributes:{currencyCode: "USD"}, editable: false }
// ];

export default class ProductPriceBook extends LightningElement {
  // @api objectApiName;
  // @api recordType;

  // @track productsInfo;

  // columns = columns;

  // @wire(getRecordsByRecordType, { premiseName: "Business premises" })
  // wiredResult({ data, error }) {
  //   if (data) {
  //     console.log(data);
  //     this.productsInfo = data;
  //   }
  //   if (error) {
  //     console.log(error);
  //   }
  // }
}